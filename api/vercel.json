{
    "version": 2,
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/index.js"
      }
    ],
    "env": {
      "DB_CONNECTION_STRING": "@db_connection_string",
      "JWT_SECRET": "@jwt_secret",
      "S3_BUCKET": "@s3_bucket"
    }
  }